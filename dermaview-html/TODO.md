# TODO

- [ ] Implement app-wide theme switching driven by `defaultTheme` in `localStorage`.
- [ ] Update `styles/global.css` to include CSS variable overrides for `clinical` and `contrast` themes.
- [ ] Update `system-branding.js` to read `dermaview.admin.systemSettings.defaultTheme` and apply it on `document.documentElement`.
- [x] Verify that changing Default Theme in admin/system settings changes appearance on:
  - [ ] `index.html`
  - [ ] `admin.html` (admin dashboard)
  - [ ] `super_admin/super-admin.html`


